/*
 * Education & Experience section — animated slider (spec v3 §1).
 * Replaced the static two-column timeline with a coverflow carousel.
 * The old static layout is preserved in git history if needed.
 */
export { JourneyCarousel as Timeline } from "./JourneyCarousel";
